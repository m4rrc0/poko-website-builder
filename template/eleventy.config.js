// Eleventy reads `dir`, `templateFormats` and `pathPrefix` from the config file
// of the project it runs in, so this thin re-export is all a website needs.
export { default, config } from "poko-website-builder/eleventy";
