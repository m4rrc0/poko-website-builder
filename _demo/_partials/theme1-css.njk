{% css %}

/* Global Theme Colors */
.theme1-v1-light {
    color: var(--color-typo);
    background-color: var(--color-contrast);
}

/* Components */
img {
    border-radius: var(--radius-max);
    max-width: 100%;
}

a {
    color: currentColor;
}

/* Header */
#menu {
    padding-block: 2rem;
}

#menu nav {
    /* Utility classes handle layout */
}

#menu a {
    text-decoration: none;
    text-transform: uppercase;
    font-size: 0.8em;
    font-weight: 600;
    letter-spacing: 0.05em;
}

main, section, article {
    margin-block: 0;
}

/* Button */
.cta-button {
    background-color: var(--color-typo);
    color: var(--color-accent); /* White text */
    padding: 0.75em 1.5em;
    border-radius: 4px; /* Matches mockup button style better than pill? Mockup looks slightly rounded */
    display: inline-flex;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.8em;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: opacity 0.2s ease;
}

.cta-button:hover {
    opacity: 0.9;
}

/* Hero */
#hero {
    /* Using border-image to create the bleed effect with a gradient */
    /* Overrides the default .bleed-bg conic-gradient */
    border-image: linear-gradient(to bottom, var(--color-contrast), var(--color-alt)) fill 0/0/0 100vw;
    padding-block: 8rem;
}

#hero h1 {
    font-size: clamp(2rem, 5vw, 4rem);
    line-height: 1.1;
    text-transform: uppercase;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
}

#hero p {
    font-size: 1.125rem;
    max-width: 40ch;
    margin-inline: auto;
    margin-bottom: 2rem;
}

/* Problem Statement */
#problem-statement {
    padding-block: 6rem;
}

#problem-statement h2 {
    text-transform: uppercase;
    font-size: 1.25rem;
    letter-spacing: 0.05em;
    margin-bottom: 1.5rem;
}

#problem-statement p {
    font-size: 0.9rem;
    line-height: 1.6;
    max-width: 60ch;
    margin-inline: auto;
}

/* About */
#about {
    /* Set --color-bg to enable the .bleed-bg utility class */
    --color-bg: var(--color-alt);
    background-color: var(--color-bg);
    padding-block: 6rem;
}

#about h3 {
    text-transform: uppercase;
    font-size: 1.25rem;
    letter-spacing: 0.05em;
    margin-bottom: 1rem;
}

.image-container {
    width: 300px;
    height: 300px;
    /* Empêche le switcher de changer la taille de cet élément */
    flex: 0 0 300px; 
    border-radius: var(--radius-max);
    overflow: hidden; /* Important si l'image dépasse les coins arrondis */
}

.image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block; /* Évite les espaces fantômes sous l'image */
}

/* Services */
#services {
    padding-block: 6rem;
}

.service-item h4 {
    margin-top: 1.5rem;
    text-transform: uppercase;
    font-size: 0.9rem;
    letter-spacing: 0.1em;
}

/* Conclusion */
#conclusion {
    padding-block: 6rem;
}

/* Footer */
#footer {
    --color-bg: var(--color-alt);
    background-color: var(--color-bg);
    padding-block: 4rem;
    font-size: 0.9rem;
}

#footer h2 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
}

.social-icons {
    gap: 1rem;
    margin-top: 1rem;
}

.social-icons a {
    font-weight: bold;
}

#footer input {
    border: none;
    padding: 0.75rem;
    background-color: var(--color-accent); /* White input */
}

{% endcss %}
